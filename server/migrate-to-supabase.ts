console.log('Supabase migration script disabled — using local JSON database for development.');
console.log('If you need to migrate data to Supabase, re-enable SUPABASE_URL and SUPABASE_ANON_KEY and restore the migration script.');
process.exit(0);
